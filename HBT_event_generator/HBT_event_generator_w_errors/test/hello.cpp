#include <iostream>
#include <cmath>

using namespace std;

int main(int argc, char * argv[])
{
	cout << "Hello, world!" << endl;

	double x = -0.0968;
	cout << x << " == " << (int)(x) << " == " << floor(x) << endl;

	return (0);
}
