#! /usr/bin/env bash

#PBS -l walltime=00:15:00
#PBS -l nodes=1:ppn=12
#PBS -A PAS0254
#PBS -j oe

cd "$PBS_O_WORKDIR" || exit $?

./compile_all.sh 12 &> compile_all.out

if [ "$?" -eq "0" ]
then
	./driver.sh &> driver.out
fi
